import React from "react";

const Loading = () => {
  return (
    <div
      style={{
        display: "flex",
        minHeight: "100vh",
        justifyContent: "center",
        alignItems: "center",
      }}
    >
      <div>
        <h1>Loading...</h1>
      </div>
    </div>
  );
};

export default Loading;
